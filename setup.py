from setuptools import setup, find_packages

setup(
    name='pipe',
    version='0.0.9',
    description='Shared library for crypto-pipe project',
    author='Yi Te',
    author_email='coastq22889@icloud.com',
    packages=find_packages(include=['pipe']),
    install_requires=[

    ],
)